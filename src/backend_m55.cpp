#include <edge/backend.hpp>

// M55-specific kernels are intentionally not implemented in v0.1. The public
// backend tag exists so future target-specific code can live outside generic
// headers without pulling vendor headers into host builds.

