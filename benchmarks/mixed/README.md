# Mixed Host/Firmware Benchmarks

Use this area for benchmark campaigns that intentionally pair host and firmware
measurements in the same experiment. Current measurements are split between
`benchmarks/host/` and `benchmarks/firmware/` because their protocols and
artifacts are separate.
